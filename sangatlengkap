<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Everyday Web Utility Hub — Mega Suite</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <!-- QR Code Library -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <!-- html2pdf Library -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Plus Jakarta Sans', 'sans-serif'],
          },
          colors: {
            app: {
              dark: '#070a13',
              card: '#0f172a',
              panel: '#1e293b'
            }
          }
        }
      }
    }
  </script>
</head>
<body class="bg-app-dark text-gray-100 font-sans antialiased min-h-screen flex flex-col">

  <!-- NAVBAR -->
  <nav class="sticky top-0 z-50 bg-app-dark/90 backdrop-blur-md border-b border-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
      <div class="flex items-center space-x-3">
        <div class="w-10 h-10 rounded-xl bg-gradient-to-tr from-blue-600 via-indigo-600 to-purple-600 flex items-center justify-center text-white font-extrabold text-xl shadow-lg shadow-blue-500/20">
          <i class="fa-solid fa-cubes text-sm"></i>
        </div>
        <div>
          <span class="text-lg font-black tracking-tight text-white block leading-none">EVERYDAY<span class="text-blue-400">MEGAHUB</span></span>
          <span class="text-[10px] text-gray-400 tracking-wider uppercase font-semibold">12-in-1 Complete Everyday Digital Suite</span>
        </div>
      </div>
      <div class="text-xs text-gray-400 font-medium hidden sm:flex items-center space-x-2">
        <span class="px-3 py-1 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/20 flex items-center space-x-1">
          <i class="fa-solid fa-shield-halved text-[10px]"></i>
          <span>100% Client-Side / Tanpa Server</span>
        </span>
      </div>
    </div>
  </nav>

  <!-- MAIN CONTAINER -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex-grow space-y-8">
    
    <!-- HEADER HERO -->
    <div class="text-center max-w-3xl mx-auto space-y-2">
      <h1 class="text-2xl sm:text-3xl font-extrabold text-white">Semua Alat Digital Harian dalam Satu Dashboard</h1>
      <p class="text-xs sm:text-sm text-gray-400">Bebas iklan, tanpa registrasi, tanpa unggah file ke internet. Semua diproses langsung di perangkat Anda secara aman.</p>
    </div>

    <!-- GRID TOOLBOX (12 TOOLS) -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">

      <!-- TOOL 1: NETWORK & IP CHECKER -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-3">
            <div class="w-8 h-8 rounded-lg bg-blue-500/10 text-blue-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-network-wired"></i>
            </div>
            <h2 class="text-sm font-bold text-white">1. Cek IP & Jaringan</h2>
          </div>
          <p class="text-xs text-gray-400 mb-3">Info IP publik, lokasi negara, dan penyedia internet saat ini.</p>
          
          <div class="bg-slate-900 border border-slate-800 rounded-xl p-3 space-y-1.5 font-mono text-xs">
            <div class="flex justify-between"><span class="text-gray-500">IP Publik:</span><span id="ipAddress" class="text-blue-400 font-bold">Memuat...</span></div>
            <div class="flex justify-between"><span class="text-gray-500">Lokasi:</span><span id="ipLocation" class="text-gray-300">Memuat...</span></div>
            <div class="flex justify-between"><span class="text-gray-500">ISP:</span><span id="ipIsp" class="text-gray-300">Memuat...</span></div>
          </div>
        </div>
        <button onclick="fetchIPInfo()" class="w-full py-2 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-rotate-right mr-1"></i> Perbarui Info
        </button>
      </div>

      <!-- TOOL 2: PASSWORD GENERATOR -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-3">
            <div class="w-8 h-8 rounded-lg bg-emerald-500/10 text-emerald-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-key"></i>
            </div>
            <h2 class="text-sm font-bold text-white">2. Generator Sandi Aman</h2>
          </div>
          <p class="text-xs text-gray-400 mb-3">Buat kata sandi acak dengan tingkat keamanan tinggi.</p>

          <div class="space-y-3">
            <div class="relative">
              <input type="text" id="passOutput" readonly class="w-full bg-slate-900 border border-slate-800 rounded-xl px-3 py-2 text-xs font-mono text-emerald-400 font-bold focus:outline-none">
              <button onclick="copyPassword()" class="absolute right-2 top-2 text-xs text-gray-400 hover:text-white">
                <i class="fa-solid fa-copy"></i>
              </button>
            </div>
            <div>
              <div class="flex justify-between text-[11px] text-gray-400 mb-1">
                <span>Panjang Karakter:</span><span id="passLenVal" class="font-bold text-white">16</span>
              </div>
              <input type="range" id="passLen" min="8" max="32" value="16" oninput="generatePassword()" class="w-full h-1 bg-slate-800 rounded-lg appearance-none cursor-pointer">
            </div>
          </div>
        </div>
        <button onclick="generatePassword()" class="w-full py-2 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-dice mr-1"></i> Acak Sandi
        </button>
      </div>

      <!-- TOOL 3: QR CODE GENERATOR & DOWNLOAD -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-3">
            <div class="w-8 h-8 rounded-lg bg-purple-500/10 text-purple-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-qrcode"></i>
            </div>
            <h2 class="text-sm font-bold text-white">3. Pembuat Kode QR</h2>
          </div>
          <p class="text-xs text-gray-400 mb-2">Ubah URL atau teks menjadi kode QR siap scan.</p>

          <input type="text" id="qrText" placeholder="Ketik URL/Teks..." oninput="generateQR()" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-3 py-1.5 text-xs text-white focus:outline-none focus:border-purple-400 mb-2">
          
          <div class="flex justify-center p-2 bg-white rounded-xl w-24 h-24 mx-auto" id="qrcode"></div>
        </div>
        <button onclick="downloadQR()" class="w-full py-1.5 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-download mr-1"></i> Unduh Gambar QR
        </button>
      </div>

      <!-- TOOL 4: TEXT UTILITY & SLUG -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-amber-500/10 text-amber-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-font"></i>
            </div>
            <h2 class="text-sm font-bold text-white">4. Pembersih & Format Teks</h2>
          </div>

          <textarea id="textInput" oninput="analyzeText()" rows="2" placeholder="Tempel teks di sini..." class="w-full bg-slate-900 border border-slate-800 rounded-xl p-2 text-xs text-white focus:outline-none focus:border-amber-400 resize-none mb-2"></textarea>

          <div class="grid grid-cols-2 gap-2 font-mono text-[10px] text-gray-400 bg-slate-900 p-2 rounded-xl border border-slate-800 mb-2">
            <div>Karakter: <span id="charCount" class="text-amber-400 font-bold">0</span></div>
            <div>Kata: <span id="wordCount" class="text-amber-400 font-bold">0</span></div>
          </div>
        </div>

        <div class="grid grid-cols-3 gap-1 text-[10px]">
          <button onclick="transformText('upper')" class="py-1 bg-slate-800 hover:bg-slate-700 text-gray-300 rounded border border-slate-700">KAPITAL</button>
          <button onclick="transformText('lower')" class="py-1 bg-slate-800 hover:bg-slate-700 text-gray-300 rounded border border-slate-700">kecil</button>
          <button onclick="transformText('slug')" class="py-1 bg-slate-800 hover:bg-slate-700 text-gray-300 rounded border border-slate-700">url-slug</button>
        </div>
      </div>

      <!-- TOOL 5: IMAGE CONVERTER & COMPRESSOR -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-rose-500/10 text-rose-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-file-image"></i>
            </div>
            <h2 class="text-sm font-bold text-white">5. Pengompres & Format Gambar</h2>
          </div>
          <p class="text-xs text-gray-400 mb-2">Ubah format JPG/PNG/WEBP atau kecilkan ukuran gambar.</p>

          <input type="file" id="imgInput" accept="image/*" class="w-full text-xs text-gray-400 bg-slate-900 rounded-xl p-1.5 border border-slate-800 mb-2">

          <div class="grid grid-cols-2 gap-2 text-xs">
            <select id="imgFormat" class="bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-white">
              <option value="image/jpeg">Format JPG</option>
              <option value="image/png">Format PNG</option>
              <option value="image/webp">Format WEBP</option>
            </select>
            <input type="number" id="imgQuality" placeholder="Kualitas 1-100" value="80" class="bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-white">
          </div>
        </div>
        <button onclick="processImage()" class="w-full py-1.5 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-gear mr-1"></i> Proses & Unduh Gambar
        </button>
      </div>

      <!-- TOOL 6: PDF GENERATOR -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-teal-500/10 text-teal-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-file-pdf"></i>
            </div>
            <h2 class="text-sm font-bold text-white">6. Generator PDF Instan</h2>
          </div>
          <p class="text-xs text-gray-400 mb-2">Ketik dokumen atau catatan singkat, langsung ekspor ke PDF.</p>

          <textarea id="pdfContent" rows="3" placeholder="Tulis teks dokumen di sini..." class="w-full bg-slate-900 border border-slate-800 rounded-xl p-2 text-xs text-white focus:outline-none focus:border-teal-400 resize-none mb-2"></textarea>
        </div>
        <button onclick="generatePDF()" class="w-full py-1.5 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-download mr-1"></i> Cetak Dokumen PDF
        </button>
      </div>

      <!-- TOOL 7: DISCOUNT & SHOPPING CALCULATOR -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-indigo-500/10 text-indigo-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-percent"></i>
            </div>
            <h2 class="text-sm font-bold text-white">7. Kalkulator Diskon</h2>
          </div>

          <div class="grid grid-cols-2 gap-2 mb-2">
            <div>
              <label class="block text-[10px] text-gray-400">Harga (Rp)</label>
              <input type="number" id="priceInput" placeholder="100000" oninput="calcDiscount()" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
            <div>
              <label class="block text-[10px] text-gray-400">Diskon (%)</label>
              <input type="number" id="discInput" placeholder="20" oninput="calcDiscount()" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
          </div>

          <div class="bg-slate-900 border border-slate-800 rounded-xl p-2 space-y-1 text-xs">
            <div class="flex justify-between text-gray-400"><span>Hemat:</span><span id="saveAmount" class="text-emerald-400 font-bold">Rp 0</span></div>
            <div class="flex justify-between text-gray-200 font-bold pt-1 border-t border-slate-800"><span>Bayar:</span><span id="finalPrice" class="text-indigo-400">Rp 0</span></div>
          </div>
        </div>
      </div>

      <!-- TOOL 8: PERCENTAGE & MARGIN CALCULATOR -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-cyan-500/10 text-cyan-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-calculator"></i>
            </div>
            <h2 class="text-sm font-bold text-white">8. Kalkulator Persentase</h2>
          </div>

          <div class="grid grid-cols-2 gap-2 mb-2">
            <div>
              <label class="block text-[10px] text-gray-400">Berapa %</label>
              <input type="number" id="pctVal" placeholder="10" oninput="calcPct()" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
            <div>
              <label class="block text-[10px] text-gray-400">Dari Total</label>
              <input type="number" id="pctTotal" placeholder="500000" oninput="calcPct()" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
          </div>

          <div class="bg-slate-900 border border-slate-800 rounded-xl p-2 text-xs flex justify-between font-mono">
            <span class="text-gray-400">Hasil:</span>
            <span id="pctResult" class="text-cyan-400 font-bold">0</span>
          </div>
        </div>
      </div>

      <!-- TOOL 9: COLOR PICKER & CONVERTER -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-pink-500/10 text-pink-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-palette"></i>
            </div>
            <h2 class="text-sm font-bold text-white">9. Pemetik Warna Kode</h2>
          </div>

          <div class="flex items-center space-x-3 mb-2">
            <input type="color" id="colorPicker" value="#3b82f6" oninput="updateColor()" class="w-10 h-10 rounded-xl bg-transparent border-0 cursor-pointer">
            <div class="flex-grow font-mono text-xs space-y-1">
              <div class="text-gray-300">HEX: <span id="hexCode" class="text-pink-400 font-bold">#3b82f6</span></div>
              <div class="text-gray-400">RGB: <span id="rgbCode">rgb(59, 130, 246)</span></div>
            </div>
          </div>
        </div>
        <button onclick="copyColor()" class="w-full py-1.5 bg-slate-800 hover:bg-slate-700 text-gray-200 text-xs font-semibold rounded-xl border border-slate-700 transition">
          <i class="fa-solid fa-copy mr-1"></i> Salin Kode HEX
        </button>
      </div>

      <!-- TOOL 10: STOPWATCH & TIMER -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-orange-500/10 text-orange-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-stopwatch"></i>
            </div>
            <h2 class="text-sm font-bold text-white">10. Timer & Stopwatch</h2>
          </div>

          <div class="text-center font-mono text-2xl font-black text-orange-400 my-2" id="timerDisplay">
            00:00:00
          </div>
        </div>

        <div class="grid grid-cols-2 gap-2 text-xs">
          <button id="btnStartTimer" onclick="toggleTimer()" class="py-1.5 bg-slate-800 hover:bg-slate-700 text-emerald-400 font-semibold rounded-xl border border-slate-700">Mulai</button>
          <button onclick="resetTimer()" class="py-1.5 bg-slate-800 hover:bg-slate-700 text-rose-400 font-semibold rounded-xl border border-slate-700">Reset</button>
        </div>
      </div>

      <!-- TOOL 11: QUICK AUTO-SAVE NOTES -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center justify-between mb-2">
            <div class="flex items-center space-x-3">
              <div class="w-8 h-8 rounded-lg bg-emerald-500/10 text-emerald-400 flex items-center justify-center text-sm font-bold">
                <i class="fa-solid fa-note-sticky"></i>
              </div>
              <h2 class="text-sm font-bold text-white">11. Catatan Otomatis</h2>
            </div>
            <span id="saveStatus" class="text-[10px] text-emerald-400 font-semibold opacity-0 transition-opacity">Tersimpan!</span>
          </div>

          <textarea id="quickNotes" oninput="saveNotes()" rows="3" placeholder="Tulis catatan... Otomatis tersimpan di browser." class="w-full bg-slate-900 border border-slate-800 rounded-xl p-2 text-xs text-white focus:outline-none focus:border-emerald-400 resize-none"></textarea>
        </div>
        <button onclick="clearNotes()" class="w-full py-1 bg-slate-800 hover:bg-slate-700 text-gray-400 hover:text-rose-400 text-[10px] font-semibold rounded-xl border border-slate-700 transition">
          Hapus Catatan
        </button>
      </div>

      <!-- TOOL 12: UNIT CONVERTER -->
      <div class="bg-app-card border border-slate-800 rounded-2xl p-6 shadow-xl flex flex-col justify-between space-y-4">
        <div>
          <div class="flex items-center space-x-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-yellow-500/10 text-yellow-400 flex items-center justify-center text-sm font-bold">
              <i class="fa-solid fa-ruler-combined"></i>
            </div>
            <h2 class="text-sm font-bold text-white">12. Konverter Panjang</h2>
          </div>

          <div class="grid grid-cols-2 gap-2 mb-2">
            <div>
              <label class="block text-[10px] text-gray-400">Meter (m)</label>
              <input type="number" id="meterInput" placeholder="1" oninput="convertUnit('m')" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
            <div>
              <label class="block text-[10px] text-gray-400">Centimeter (cm)</label>
              <input type="number" id="cmInput" placeholder="100" oninput="convertUnit('cm')" class="w-full bg-slate-900 border border-slate-800 rounded-xl px-2 py-1 text-xs text-white">
            </div>
          </div>

          <div class="bg-slate-900 border border-slate-800 rounded-xl p-2 text-xs flex justify-between font-mono">
            <span class="text-gray-400">Kilometer (km):</span>
            <span id="kmResult" class="text-yellow-400 font-bold">0 km</span>
          </div>
        </div>
      </div>

    </div>

  </main>

  <!-- FOOTER -->
  <footer class="bg-slate-950 border-t border-slate-800 py-6 text-center text-xs text-gray-500">
    <p>© 2026 Everyday Web Utility Hub — Mega Suite Edition • Single File Web App</p>
  </footer>

  <!-- SCRIPT LOGIC -->
  <script>
    // 1. IP CHECKER
    async function fetchIPInfo() {
      try {
        const res = await fetch('https://ipapi.co/json/');
        const data = await res.json();
        document.getElementById('ipAddress').innerText = data.ip || 'N/A';
        document.getElementById('ipLocation').innerText = `${data.city || ''}, ${data.country_name || ''}`;
        document.getElementById('ipIsp').innerText = data.org || 'N/A';
      } catch (err) {
        document.getElementById('ipAddress').innerText = 'Gagal memuat';
        document.getElementById('ipLocation').innerText = 'Tidak tersedia';
        document.getElementById('ipIsp').innerText = 'Tidak tersedia';
      }
    }

    // 2. PASSWORD GENERATOR
    function generatePassword() {
      const len = document.getElementById('passLen').value;
      document.getElementById('passLenVal').innerText = len;
      const chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*()_+-=";
      let pass = "";
      for (let i = 0; i < len; i++) {
        pass += chars.charAt(Math.floor(Math.random() * chars.length));
      }
      document.getElementById('passOutput').value = pass;
    }

    function copyPassword() {
      const passInput = document.getElementById('passOutput');
      passInput.select();
      navigator.clipboard.writeText(passInput.value);
      alert('Sandi berhasil disalin!');
    }

    // 3. QR GENERATOR & DOWNLOAD
    function generateQR() {
      const text = document.getElementById('qrText').value.trim() || "https://google.com";
      const qrContainer = document.getElementById('qrcode');
      qrContainer.innerHTML = "";
      new QRCode(qrContainer, { text: text, width: 96, height: 96 });
    }

    function downloadQR() {
      const img = document.querySelector('#qrcode img');
      if (img) {
        const a = document.createElement('a');
        a.href = img.src;
        a.download = 'qrcode.png';
        a.click();
      }
    }

    // 4. TEXT UTILITY
    function analyzeText() {
      const val = document.getElementById('textInput').value;
      document.getElementById('charCount').innerText = val.length;
      const words = val.trim() ? val.trim().split(/\s+/).length : 0;
      document.getElementById('wordCount').innerText = words;
    }

    function transformText(type) {
      const input = document.getElementById('textInput');
      if (type === 'upper') input.value = input.value.toUpperCase();
      if (type === 'lower') input.value = input.value.toLowerCase();
      if (type === 'slug') {
        input.value = input.value.toLowerCase().replace(/[^a-z0-9 -]/g, '').replace(/\s+/g, '-').replace(/-+/g, '-');
      }
      analyzeText();
    }

    // 5. IMAGE CONVERTER & COMPRESSOR
    function processImage() {
      const file = document.getElementById('imgInput').files[0];
      if (!file) return alert('Pilih gambar terlebih dahulu!');

      const format = document.getElementById('imgFormat').value;
      const quality = parseFloat(document.getElementById('imgQuality').value) / 100;

      const reader = new FileReader();
      reader.onload = function(e) {
        const img = new Image();
        img.onload = function() {
          const canvas = document.createElement('canvas');
          canvas.width = img.width;
          canvas.height = img.height;
          const ctx = canvas.getContext('2d');
          ctx.drawImage(img, 0, 0);

          const dataUrl = canvas.toDataURL(format, quality);
          const a = document.createElement('a');
          a.href = dataUrl;
          a.download = `converted-image.${format.split('/')[1]}`;
          a.click();
        };
        img.src = e.target.result;
      };
      reader.readAsDataURL(file);
    }

    // 6. PDF GENERATOR
    function generatePDF() {
      const text = document.getElementById('pdfContent').value.trim();
      if (!text) return alert('Masukkan teks dokumen terlebih dahulu!');

      const element = document.createElement('div');
      element.style.padding = '20px';
      element.style.fontFamily = 'sans-serif';
      element.innerText = text;

      html2pdf().from(element).save('dokumen.pdf');
    }

    // 7. DISCOUNT CALCULATOR
    function calcDiscount() {
      const price = parseFloat(document.getElementById('priceInput').value) || 0;
      const disc = parseFloat(document.getElementById('discInput').value) || 0;
      const save = (price * disc) / 100;
      document.getElementById('saveAmount').innerText = `Rp ${save.toLocaleString('id-ID')}`;
      document.getElementById('finalPrice').innerText = `Rp ${(price - save).toLocaleString('id-ID')}`;
    }

    // 8. PERCENTAGE CALCULATOR
    function calcPct() {
      const val = parseFloat(document.getElementById('pctVal').value) || 0;
      const total = parseFloat(document.getElementById('pctTotal').value) || 0;
      document.getElementById('pctResult').innerText = ((val / 100) * total).toLocaleString('id-ID');
    }

    // 9. COLOR PICKER
    function updateColor() {
      const hex = document.getElementById('colorPicker').value;
      document.getElementById('hexCode').innerText = hex;

      const r = parseInt(hex.slice(1, 3), 16);
      const g = parseInt(hex.slice(3, 5), 16);
      const b = parseInt(hex.slice(5, 7), 16);
      document.getElementById('rgbCode').innerText = `rgb(${r}, ${g}, ${b})`;
    }

    function copyColor() {
      const hex = document.getElementById('hexCode').innerText;
      navigator.clipboard.writeText(hex);
      alert(`Kode warna ${hex} disalin!`);
    }

    // 10. STOPWATCH
    let timerInterval = null;
    let seconds = 0;

    function toggleTimer() {
      const btn = document.getElementById('btnStartTimer');
      if (timerInterval) {
        clearInterval(timerInterval);
        timerInterval = null;
        btn.innerText = "Mulai";
        btn.className = "py-1.5 bg-slate-800 hover:bg-slate-700 text-emerald-400 font-semibold rounded-xl border border-slate-700";
      } else {
        timerInterval = setInterval(() => {
          seconds++;
          const hrs = String(Math.floor(seconds / 3600)).padStart(2, '0');
          const mins = String(Math.floor((seconds % 3600) / 60)).padStart(2, '0');
          const secs = String(seconds % 60).padStart(2, '0');
          document.getElementById('timerDisplay').innerText = `${hrs}:${mins}:${secs}`;
        }, 1000);
        btn.innerText = "Jeda";
        btn.className = "py-1.5 bg-slate-800 hover:bg-slate-700 text-amber-400 font-semibold rounded-xl border border-slate-700";
      }
    }

    function resetTimer() {
      clearInterval(timerInterval);
      timerInterval = null;
      seconds = 0;
      document.getElementById('timerDisplay').innerText = "00:00:00";
      document.getElementById('btnStartTimer').innerText = "Mulai";
    }

    // 11. QUICK NOTES
    function saveNotes() {
      localStorage.setItem('megahub_notes', document.getElementById('quickNotes').value);
      const status = document.getElementById('saveStatus');
      status.style.opacity = '1';
      setTimeout(() => { status.style.opacity = '0'; }, 1000);
    }

    function loadNotes() {
      const saved = localStorage.getItem('megahub_notes');
      if (saved) document.getElementById('quickNotes').value = saved;
    }

    function clearNotes() {
      document.getElementById('quickNotes').value = '';
      localStorage.removeItem('megahub_notes');
    }

    // 12. UNIT CONVERTER
    function convertUnit(type) {
      if (type === 'm') {
        const m = parseFloat(document.getElementById('meterInput').value) || 0;
        document.getElementById('cmInput').value = m * 100;
        document.getElementById('kmResult').innerText = `${m / 1000} km`;
      } else {
        const cm = parseFloat(document.getElementById('cmInput').value) || 0;
        document.getElementById('meterInput').value = cm / 100;
        document.getElementById('kmResult').innerText = `${cm / 100000} km`;
      }
    }

    // INITIALIZATION
    window.onload = function() {
      fetchIPInfo();
      generatePassword();
      generateQR();
      loadNotes();
    };
  </script>
</body>
</html>
